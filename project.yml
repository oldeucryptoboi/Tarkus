name: Tarkus
options:
  bundleIdPrefix: com.oldeucryptoboi
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    TARGETED_DEVICE_FAMILY: "1"
    ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    DEVELOPMENT_TEAM: ""

targets:
  Tarkus:
    type: application
    platform: iOS
    sources:
      - path: Tarkus
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.oldeucryptoboi.Tarkus
        INFOPLIST_FILE: Tarkus/Info.plist
        GENERATE_INFOPLIST_FILE: false
        DEVELOPMENT_ASSET_PATHS: "\"Tarkus/Preview Content\""
    scheme:
      testTargets:
        - TarkusTests

  TarkusTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: TarkusTests
    dependencies:
      - target: Tarkus
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.oldeucryptoboi.TarkusTests
        GENERATE_INFOPLIST_FILE: true
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Tarkus.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/Tarkus"
        BUNDLE_LOADER: "$(TEST_HOST)"
