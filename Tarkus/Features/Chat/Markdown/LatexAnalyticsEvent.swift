import Foundation

// MARK: - LatexAnalyticsEvent

/// Mirrors OpenAI's `OAIMarkdown.LatexAnalyticsEvent`.
/// Cases are not reconstructible from public symbols — placeholder enum.
enum LatexAnalyticsEvent: Hashable {
    // Stub — cases will be added when LaTeX analytics are implemented
}
